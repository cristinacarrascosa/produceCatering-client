import { ShowDateTimePipe } from './show-date-time.pipe';

describe('ShowDateTimePipe', () => {
  it('create an instance', () => {
    const pipe = new ShowDateTimePipe();
    expect(pipe).toBeTruthy();
  });
});
